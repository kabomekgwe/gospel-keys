/**
 * Gospel Keys Design System
 *
 * A comprehensive, atomic design system for the Gospel Keys music education platform.
 *
 * Structure:
 * - tokens: Design tokens (colors, typography, spacing, etc.)
 * - atoms: Basic building blocks (Button, Badge, Progress, etc.)
 * - molecules: Composed components (Card, ExerciseCard, CurriculumCard, etc.)
 * - templates: Layout components (Container, Stack, Grid)
 *
 * Usage:
 * ```tsx
 * import { Button, Badge, ExerciseCard, Container } from '@/design-system';
 * ```
 */

// Tokens
export * from './tokens';

// Atoms
export * from './components/atoms';

// Molecules
export * from './components/molecules';

// Templates
export * from './components/templates';
